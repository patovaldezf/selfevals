#!/usr/bin/env bash
set -euo pipefail

uv run python scripts/audit_technical_debt.py --fail-on-regression
uv run ruff check .

if [ -d web ] && [ -f web/package.json ]; then
  (cd web && npm run lint)
fi
